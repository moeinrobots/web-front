"use client";
import {useState} from "react";
export function ContactForm(){const [status,setStatus]=useState<"idle"|"sending"|"done"|"error">("idle");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setStatus("sending");const formElement=event.currentTarget;const form=new FormData(formElement);const payload={name:String(form.get("name")||""),phone:String(form.get("phone")||""),email:String(form.get("email")||""),projectType:String(form.get("projectType")||""),message:String(form.get("message")||""),website:String(form.get("website")||"")};try{const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL||"http://localhost:4000/api"}/leads`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload),signal:AbortSignal.timeout(12000)});if(!response.ok)throw new Error();setStatus("done");formElement.reset()}catch{setStatus("error")}}
return <form className="contact-form" onSubmit={submit}><h2>اطلاعات پروژه</h2><input className="hp" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><div className="form-row"><label>نام و نام خانوادگی<input name="name" required minLength={2} placeholder="مثلاً سارا احمدی"/></label><label>شماره تماس<input name="phone" required minLength={8} inputMode="tel" placeholder="۰۹۱۲۱۲۳۴۵۶۷"/></label></div><label>ایمیل<input name="email" type="email" placeholder="name@company.com"/></label><label>نوع پروژه<select name="projectType"><option>طراحی سایت شرکتی</option><option>فروشگاه اینترنتی</option><option>طراحی محصول اختصاصی</option><option>بازطراحی و بهبود سایت</option><option>سئو و بهینه‌سازی</option></select></label><label>درباره پروژه<textarea name="message" rows={6} required minLength={10} placeholder="کمی درباره کسب‌وکار، اهداف و امکانات موردنیاز بنویسید..."/></label><button className="btn primary" type="submit" disabled={status==="sending"}>{status==="sending"?"در حال ارسال…":"ارسال درخواست مشاوره"}</button><div className={`form-message ${status}`} role="status">{status==="done"&&"درخواست شما ثبت شد؛ خیلی زود با شما تماس می‌گیریم."}{status==="error"&&"ارسال انجام نشد. لطفاً دوباره تلاش کنید یا با ما تماس بگیرید."}</div></form>}
